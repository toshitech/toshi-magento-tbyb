define([
    "jquery"
  ], 
  function($) {
    "use strict";
    // Temporarily hide Toshi shipping for multiple address checkout
    // if (jQuery("[value=toshi_toshi]").closest('form').hasClass('multicheckout')){
    //     jQuery("[value=toshi_toshi]").closest('.item-content').hide();
    //     jQuery("[value=toshi_toshi]").closest('dd').prev('.item-title').hide();
    // }
  });